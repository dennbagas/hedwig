services:
  hedwig:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./config.yaml:/app/config.yaml
      - ./hedwig-bot.private-key.pem:/etc/hedwig/github-app.pem
      - ./hedwig-dev.db:/data/hedwig.db
    restart: unless-stopped
