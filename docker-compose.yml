services:
  hedwig:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./config.yaml:/app/config.yaml
      - ./hedwig-bot.2026-07-09.private-key.pem:/app/github.pem
      - hedwig-data:/data
    restart: unless-stopped

volumes:
  hedwig-data:
